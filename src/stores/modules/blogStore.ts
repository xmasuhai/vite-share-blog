import {defineStore} from 'pinia';

const state = () => {
  return {};
};
const getters = {};
const actions = {};

export const useBlogStore = defineStore('blogStore', {
  state,
  getters,
  actions,
});
